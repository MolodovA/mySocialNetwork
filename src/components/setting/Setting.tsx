import React from 'react';

import { Title } from 'components/common/title/Title';

type SettingPropsType = {};
export const Setting = (props: SettingPropsType): any => (
  <div>
    <Title title="Setting" />
  </div>
);
